'use client';
import Link from 'next/link';
import { CalendarDays } from 'lucide-react';
import { useI18n } from '@/components/i18n-provider';
import { businessConfig } from '@/lib/business-config';
export function ConsultationBooking(){const{locale}=useI18n();const ar=locale==='ar';return <section className="section-padding bg-surface"><div className="container-max px-4 sm:px-6 lg:px-8"><div className="rounded-3xl border border-surface-border bg-surface-card p-7 text-center sm:p-10"><CalendarDays className="mx-auto h-8 w-8 text-brand-cyan-400"/><h2 className="mt-4 font-display text-2xl font-bold text-white sm:text-3xl">{ar?'احجز استشارة أولية':'Book an initial consultation'}</h2><p className="mx-auto mt-4 max-w-2xl leading-7 text-ink-secondary">{ar?'ناقش الهدف والنطاق والأولويات قبل البدء. إذا لم يتوفر رابط الحجز، أرسل طلب استشارة وسنتواصل معك.':'Discuss goals, scope, and priorities before starting. If direct booking is unavailable, send a consultation enquiry.'}</p>{businessConfig.bookingUrl?<a href={businessConfig.bookingUrl} target="_blank" rel="noopener noreferrer" className="mt-6 inline-flex min-h-[48px] items-center rounded-xl bg-white px-6 font-semibold text-surface">{ar?'افتح صفحة الحجز':'Open booking page'}</a>:<Link href={`/${locale}/contact`} className="mt-6 inline-flex min-h-[48px] items-center rounded-xl bg-white px-6 font-semibold text-surface">{ar?'أرسل طلب استشارة':'Send a consultation request'}</Link>}</div></div></section>}
