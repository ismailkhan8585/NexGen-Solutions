import Link from 'next/link'; import { headers } from 'next/headers';
export default function NotFound(){const locale=headers().get('x-site-locale')==='en'?'en':'ar';const ar=locale==='ar';return <main dir={ar?'rtl':'ltr'} className="flex min-h-screen items-center justify-center bg-surface px-4"><div className="max-w-xl text-center"><p className="font-display text-7xl font-bold text-brand-cyan-400">404</p><h1 className="mt-5 font-display text-3xl font-bold text-white">{ar?'الصفحة غير موجودة':'Page not found'}</h1><p className="mt-4 leading-7 text-ink-secondary">{ar?'قد يكون الرابط تغير أو أن الصفحة لم تعد متاحة. استخدم التنقل الآمن إلى الرئيسية أو صفحة التواصل.':'The link may have changed or the page is unavailable. Continue safely to the homepage or contact page.'}</p><div className="mt-7 flex justify-center gap-3"><Link href={`/${locale}`} className="inline-flex min-h-[48px] items-center rounded-xl bg-white px-6 font-semibold text-surface">{ar?'الرئيسية':'Home'}</Link><Link href={`/${locale}/contact`} className="inline-flex min-h-[48px] items-center rounded-xl border border-surface-border px-6 font-semibold text-white">{ar?'تواصل معنا':'Contact us'}</Link></div></div></main>}
